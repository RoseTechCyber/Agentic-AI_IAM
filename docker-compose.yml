services:
  iam-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./data/iam.db
      RISK_THRESHOLD: "70"
      LLM_BASE_URL: ""
      LLM_MODEL: ""
    volumes:
      - iam-data:/app/data

volumes:
  iam-data:
